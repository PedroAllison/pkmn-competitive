import { UnderConstruction } from '../components/UnderConstruction';

/** Calculadoras: dano, speed, IV, EV, cobertura de tipo, nature. */
export function ToolsPage() {
  return (
    <UnderConstruction
      title="Ferramentas"
      message="Calculadoras de dano, speed, IV/EV e cobertura de tipo — próxima etapa."
    />
  );
}
